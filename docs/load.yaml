phantom:
  address: burger.flexkode.ru:443
  uris:
    - /api/ingredients
  load_profile:
    load_type: rps # schedule load by defining requests per second
    schedule: line(200, 400, 2m)
  ssl: true
console:
  enabled: true # enable console output
telegraf:
  enabled: false # disable telegraf monitoring
